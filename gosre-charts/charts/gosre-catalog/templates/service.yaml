apiVersion: v1
kind: Service
metadata:
  name: {{ include "gosre-catalog.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gosre-catalog.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "gosre-catalog.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 8081
      targetPort: http
      protocol: TCP
