apiVersion: v1
kind: Service
metadata:
  name: {{ include "gosre-api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gosre-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gosre-api.selectorLabels" . | nindent 4 }}
