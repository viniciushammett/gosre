apiVersion: v1
kind: Secret
metadata:
  name: {{ include "gosre-agent.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gosre-agent.labels" . | nindent 4 }}
type: Opaque
data:
  api-key: {{ .Values.apiKey | b64enc | quote }}
