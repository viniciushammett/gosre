apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gosre-scheduler.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gosre-scheduler.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "gosre-scheduler.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "gosre-scheduler.labels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: gosre-scheduler
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: GOSRE_API_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "gosre-scheduler.fullname" . }}
                  key: GOSRE_API_URL
            - name: NATS_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "gosre-scheduler.fullname" . }}
                  key: NATS_URL
            - name: SCHEDULER_INTERVAL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "gosre-scheduler.fullname" . }}
                  key: SCHEDULER_INTERVAL
            - name: GOSRE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "gosre-scheduler.fullname" . }}
                  key: api-key
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "gosre-scheduler.fullname" . }}
                  key: redis-url
          livenessProbe:
            exec:
              command: ["true"]
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
