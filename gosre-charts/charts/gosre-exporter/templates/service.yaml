apiVersion: v1
kind: Service
metadata:
  name: {{ include "gosre-exporter.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gosre-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "gosre-exporter.selectorLabels" . | nindent 4 }}
